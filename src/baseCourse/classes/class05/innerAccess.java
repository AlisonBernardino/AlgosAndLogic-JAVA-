package baseCourse.classes.class05;

// This is a brand new system

public interface innerAccess {
    /*
    @param String userPassword = A 255 alphanumeric characters long password can be created
    @return This returns true if the password is correct
     */

    public boolean authenticatePassword(String insertedPassword);
}
